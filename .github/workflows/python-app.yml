# This workflow will install Python dependencies, run tests and lint with a single version of Python
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Python application

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: windows-latest

    steps:
    - name: install prereqs (Windows)
      run: winget install ffmpeg --disable-interactivity --accept-source-agreements --accept-package-agreements

    - name: FFMPEG_ROOT Windows
      run: echo "FFMPEG_ROOT=$env:LOCALAPPDATA/Microsoft/WinGet/Links/" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append
    - name: Print stuff
      run: |
        dir C:\Users\runneradmin\AppData\Local/Microsoft/WinGet/Links/
      
    - name: Test ffmpeg
      run: |
        C:\Users\runneradmin\AppData\Local/Microsoft/WinGet/Links/ffmpeg.exe -h
      
    - uses: actions/checkout@v5
    - name: Set up Python 3.12
      uses: actions/setup-python@v3
      with:
        python-version: "3.12"
    - name: Install uv
      uses: astral-sh/setup-uv@v6
    - name: Yolo
      run: |
        uv run python3 ffmpeg-test.py
